# 医保项目目录衔接

本项目维护国家医保医疗服务项目与省级项目之间的衔接关系。目录和映射均有生效起止时间，支持一对多、多对一及未决状态。历史费用始终按就医发生时有效的版本解释。

映射建议由专家提交，业务审核与财务审核分别确认。影子运行使用脱敏账单比较覆盖率和支付差异，不写入正式结算。首批领域片段包括心血管、呼吸、眼科、妇科、麻醉、护理和康复等类别。

使用 `python3 service.py --check` 检查基础配置，`python3 service.py --port 8000` 启动健康服务。
